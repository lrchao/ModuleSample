<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!-- 实心 -->
    <solid android:color="@color/dialog_btn_confirm_bg"/>
    <stroke
        android:width="1dp"
        android:color="@color/dialog_btn_confirm_border"/>
    <!-- 圆角 -->
    <corners android:radius="2dp"/>

    <!-- 内间距 -->
    <padding
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"/>
</shape>